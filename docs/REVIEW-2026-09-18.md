# Review — 2026-09-18 (falsification bar formally tripped; fallback starts)

Sample: **250 trades, 2026-09-04 08:20 → 2026-09-18 06:43 UTC** (16,366
1-min bars). Day 3.5 of the +0.75R ratchet era (n=75, up from 58 at the
09-17 review). One formal outcome this cycle: **the pre-registered
falsification bar tripped on 09-17 16:17 UTC and remains breached at n=75**.
Per the pre-registered plan the fallback now starts — first step: the
**~4 h max-hold time stop**, measured in isolation.

Reproduce: `python3 tools/check_data.py` (0 fail / 8 warn),
`python3 tools/win_rate_report.py`, `phantom_trades.py`, `analyze_losers.py`,
`validate_gates.py`, `smoke_test.py` (SMOKE TEST PASSED, A–J). Era split by
entry time: 0.75R era = entries ≥ 2026-09-15 06:00 UTC, now n=75.

---

## 1. Headline numbers

| Metric | All 250 | 0.75R era (n=75) |
|---|---|---|
| Result | 34W/91L/125BE | 16W/32L/27BE |
| Decisive WR — Wilson 95% | 27.2% [20.2, 35.6] | **33.3% [21.7, 47.5]** |
| Scratch rate | 50.0% | **36.0%** |
| P&L | −$157.43 | **−$33.23** |
| $/trade | −$0.63 | **−$0.443** (09-17: −$0.557) |
| R/trade | — | **−0.107R** |
| Median hold TP / SL / BE (min) | 10.0 / 6.0 / 2.0 | 7.0 / 9.0 / 9.0 |

True equity $342.57 (from $500); engine ledger $367.27 (drift +$24.70,
unchanged — known issue). Breakeven at 1:1.5 needs 40.0% decisive; the era
sits at 33.3%.

| Day | n | Result | dec WR | P&L | $/trade |
|---|---|---|---|---|---|
| 09-15 (from 06:17) | 21 | 4W/6L/11BE | 40.0% | −$1.07 | −$0.051 |
| 09-16 (FOMC) | 24 | 4W/12L/8BE | 25.0% | −$22.92 | −$0.955 |
| 09-17 | 18 | 4W/10L/4BE | 29.0% | −$14.74 | −$0.819 |
| 09-18 YTD | 12 | 4W/4L/4BE | 50.0% | +$5.50 | +$0.458 |

---

## 2. The bar is formally tripped — the fallback starts

Pre-registered at the 09-16/09-17 checks: **if 0.75R-era P&L/trade ≤ −$0.40
once n ≥ 60 → begin the pre-registered fallback — the ~4 h max-hold time
stop measured in isolation first, ratchet-off second (replay
+$0.40–0.52/trade, ~41% decisive), never back to 0.30R.**

- 60th era entry: **2026-09-17 16:17 UTC** → P&L at that point −$35.69 →
  **−$0.595/trade. Tripped.**
- Now n=75: −$33.23 → **−$0.443/trade** — still below the bar, but improving
  over the four days (−0.595 → −0.443), and 09-18 is the first positive era
  day (+$5.50 in 6.7 h).

Statistical honesty: era mean −$0.443, sd $3.72, 95% CI [−$1.29, +$0.40].
The CI still straddles zero both ways — the pre-registration is exactly what
justifies acting on the point estimate rather than the CI, and exactly why
the plan starts from the *narrowest* change (one exit-time cap) measured in
isolation, rather than rewriting the exit stack.

The two >60-min holds the stop targets are unchanged: #95 (49.1 h, weekend
gap, pre-era) and #187 (65 min, in-era). The stop also operationalises
"close before the next scheduled macro event" without any news sourcing
(FOMC gap, 09-17 review §2).

---

## 3. 09-18 partial day: first positive era day — don't over-read

- 12 trades in 6.7 h, 4W/4L/4BE, 50.0% decisive, +$5.50 (+$0.458/trade).
- Pace is stable: 169 trades in the last 7 days, 73 in the last 3 (≈24/day).
  Trade *count* is no longer the binding constraint — see §5.
- One positive day is noise against −1.07 / −22.92 / −14.74. The verdict
  belongs to the next full re-review (~09-29, or max-hold re-baseline +
  ~2 weeks in isolation, whichever comes first).

---

## 4. Queue unchanged — no new adoption bars

- **(b) BE resets the SL streak** — still queued, still down-graded (the
  FOMC-day counterweight stands: escalation covered 17:28–18:28 UTC, the one
  hour it mattered most).
- **(d) MAX_ATR 4.50 → 2.50** — still eligible, still held: the era
  ATR ≥ 2.5 bucket is unchanged at n=9, 1W/4L/4BE, **−$12.35** (no new
  trades in the bucket this cycle; n too small).
- **RSI ≥ 45** — still ~dropped. **Scheduled-news gate** — still a named
  candidate only.
- Nothing in the +17-trade window moves any of these; the only formal
  outcome this cycle is §2.

---

## 5. Realistic timeline to auto-trade (what "enough data" means)

Recorded here so future sessions inherit the framework instead of
re-deriving it (user question, 2026-09-18).

**Pace:** ~24–25 trades/day, stable (169/7 d, 73/3 d). Sample size →
calendar at ~24.8/day:

| Milestone | Data needed | At pace | Calendar |
|---|---|---|---|
| Max-hold fallback — start | n=60 era trades | **done** | **tripped 09-17 16:17 UTC** |
| 95% CI ±$0.40/trade on era mean (measurable) | ~333 era trades (~258 more) | ~11 d | late Sep |
| One full macro cycle past the FOMC (CPI + NFP) | ~700 era trades | ~4 wks | early Oct |
| 95% CI ±$0.20/trade (can *confirm* a +$0.2 edge) | ~1,330 era trades | ~8 wks | mid Nov |
| Multi-regime: ~4 FOMCs + one EMA50/200 regime flip | — | 3–4 mo | Dec 2026–Jan 2027 |
| Live pilot (micro size; broker-side BE modify needed first) | 4–8 wks live | — | Q1 2027 |
| **Auto-trade decision** | durable net-of-cost positive expectancy | **6–12 mo** | **Mar–Sep 2027** |

**What is actually binding (not trade count):**

1. **A configuration with demonstrated positive expectancy in isolation** —
   the era mean is −$0.443/trade and 33.3% WR vs the 40% breakeven.
2. **Regime coverage** — 14 trading days = one FOMC; a trend flip
   (EMA50/EMA200 cross) may be months away and the entry logic is
   entirely regime-dependent.
3. **Costs** — forward-test P&L is price delta only
   (`engine.py`: `profit = price − entry_price`); commission/spread are
   zero. A real account must be positive *after* those, so the sim edge has
   to clear broker costs with margin to spare.
4. **Sim ≠ live fills** — the LIVE-mode broker-side BE modify is
   unimplemented (engine-side only today); the pilot must verify fills
   side-by-side.

**Phase gates:**

1. Now → deploy the ~4 h max-hold stop (dedicated code session; the era
   re-baselines at deploy via autosync ≤ 3 h).
2. Deploy + ~2 weeks → judge max-hold in isolation: P&L/trade trend,
   >60-min holds ≈ 0.
3. ~8 weeks (mid Nov) → if era P&L/trade > 0 with the CI excluding ≈ 0,
   hold the config and let the regime-coverage clock run; otherwise
   fallback step 2 (ratchet-off).
4. 3–4 months → multi-regime stability.
5. Q1 2027 → micro-size live pilot.
6. 6–12 months → durable net-of-cost positive expectancy → auto-trade
   decision. Until then: forward test, not income.

---

## 6. Data hygiene

- `smoke_test.py` PASSED A–J; `py_compile` clean on `engine.py`,
  `trade_filter.py`, all of `tools/`.
- `check_data.py`: **0 fail / 8 warn**, no new species (16,366 log rows;
  schema intact; status.json agrees with trades.csv).
- No duplicate Trade_Num added (the 4 known remain); long holds, drift, and
  the era ATR bucket all as reported in §1–§2.

## 7. Decision

1. **Falsification bar formally tripped** (n=60 on 09-17 16:17 UTC at
   −$0.595/trade; −$0.443 at n=75). Per the pre-registered plan the
   fallback is active: **first step = the ~4 h max-hold time stop, measured
   in isolation**, to be implemented in the next code session (this session
   is docs-only).
2. **No other param or code changes today.** (b) remains down-graded,
   (d) held, RSI ≥ 45 ~dropped, scheduled-news gate named-only.
3. **Next re-review ≈ 09-29** or max-hold re-baseline + ~2 weeks
   in-isolation data, whichever comes first; judged on P&L/day and
   bleed/trade, not WR.
4. **Auto-trading stays a 6–12 month horizon (Mar–Sep 2027)**; the binding
   constraints are edge + regime coverage + unmodelled costs, not trade
   count (§5). Do not treat the bot as income.
